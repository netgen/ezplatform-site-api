parameters:
    netgen.ezplatform_site.prioritized_languages:
        - 'eng-GB'
        - 'ger-DE'

services:
    # In ezpublish-kernel this is defined in bundle, however we use it in
    # core implementation here
    ezpublish.query_type.registry:
        class: eZ\Publish\Core\QueryType\ArrayQueryTypeRegistry
